import { PasswordPattrenDirective } from './password-pattren.directive';

describe('PasswordPattrenDirective', () => {
  it('should create an instance', () => {
    const directive = new PasswordPattrenDirective();
    expect(directive).toBeTruthy();
  });
});
